/**
 * Sets params for an array object at the top level
 * @param {object[]} requests
 * @param {integer}  requests[].index
 * @param {object}   requests[].delta
 * @param {?string}  requests[].delta.role
 * @param {?string}  requests[].delta.content
 */
export async function GET (requests = []) {
  return true;
};
/**
 * Invalid because can't provide schema to a number for beta.obj
 * @param {string} alpha
 * @param {object} beta
 * @param {number} beta.str
 * @param {number} beta.obj
 * @param {number} beta.obj.num
 * @param {string} beta.obj.str
 * @param {array} gamma
 */
export default async (alpha, beta, gamma) => {
  /* do nothing */
}
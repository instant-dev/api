/**
 * My function
 * @param {string} alpha
 * @param {object} beta
 * @param {number} beta.num
 * @param {object} beta.obj
 * @param {number} beta.obj.num
 * @param {string} beta.obj.str
 * @param {array} gamma
 */
export default async (alpha, beta, gamma) => {
  
  return {
    alpha,
    beta,
    gamma
  };

}